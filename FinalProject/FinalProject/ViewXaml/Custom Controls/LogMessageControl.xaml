<UserControl x:Class="FinalProject.LogMessageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinalProject"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderThickness="2" BorderBrush="Blue" Background="LightBlue" CornerRadius="10,10,10,10">
        <StackPanel>
            <Label Content="{Binding Username}" Margin="5" VerticalAlignment="Center"/>
            <Label Content="{Binding Text}" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>
