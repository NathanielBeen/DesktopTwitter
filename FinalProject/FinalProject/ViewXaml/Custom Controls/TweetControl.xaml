<UserControl x:Class="FinalProject.TweetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:local="clr-namespace:FinalProject"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Grid.Row="0" HorizontalAlignment="Center" Background="LightGray" Content="{Binding Path=Username}" Command="{Binding SelectUserCommand}"/>
        <Label Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Content="{Binding Path=Text}"/>
        <Label Grid.Column="0" Grid.Row="2" HorizontalAlignment="Center" Content="{Binding Path=Retweets}"/>
        <Label Grid.Column="1" Grid.Row="2" HorizontalAlignment="Center" Content="{Binding Path=Likes}"/>
    </Grid>
</UserControl>
